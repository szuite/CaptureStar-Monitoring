
import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'custom_decorations.dart';
/*
class CircleBoxForm extends StatelessWidget {
  final String? title;

  const CircleBoxForm({Key? key, @required this.title}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context).textTheme;

    return Container(
      width: MediaQuery.of(context).size.width,
      decoration: CustomDecorations().roundFrameBox(),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        mainAxisAlignment: MainAxisAlignment.spaceBetween,
        children: [
          Padding(
            padding: const EdgeInsets.only(
              left: 28.0,
              right: 28.0,
              top: 20.0,
            ),
            child: Row(
              children: [
                Text(
                  title!,
                  style: theme.headline3,
                ),
              ],
            ),
          ),

          const Padding(
            padding: EdgeInsets.only(left: 28.0, right: 28.0, top: 28.0, bottom: 20.0),
            child: WeatherInfoForm(),
          )
        ],
      ),
    );
  }
}
*/
